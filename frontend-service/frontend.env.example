# Copy this to .env and fill in values

# Backend URL — local dev
VITE_API_URL=http://localhost:8000

# Backend URL — production (update after Fly.io deploy)
# VITE_API_URL=https://shift-ai-backend.fly.dev
